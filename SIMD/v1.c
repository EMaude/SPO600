#include <stdio.h>
#include <stdlib.h>

#define SIZE 10000000

int main() {
	uint i, t=0;
	uint* a;
	a = (uint*) calloc(SIZE, sizeof(uint));

	srand(0);

	for(i=0; i<SIZE; i++)
	{

	}

	for(i=0; i<SIZE; i++)
	{

	}

	
}